<%= form_with(model: pack, local: true) do |form| %>
  <% if pack.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(pack.errors.count, "error") %> prohibited this pack from being saved:</h2>

      <ul>
      <% pack.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :type_id %>
    <%= form.text_area :type_id %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

<% field_value = sprintf('%.2f', @inventory.sum.nil? ? 0.0 : @inventory.sum).gsub(/(\d)(?=(\d\d\d)+(?!\d))/, "\\1 ")%>
total_sum = document.getElementById("total_sum")
total_sum.innerHTML = "<%= field_value %>"
